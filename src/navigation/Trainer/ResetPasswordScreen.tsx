import * as React from 'react';
import {ResetPassword} from '@containers/Trainer';


export default function ResetPasswordScreen({navigation}) {
  return (
      <ResetPassword navigation={navigation}/>
  );
}
