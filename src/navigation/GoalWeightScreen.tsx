import * as React from 'react';
import {GoalWeight} from '@containers';

export default function GoalWeightScreen({navigation,route}) {
  return (
      <GoalWeight navigation={navigation} route={route}/>
  );
}

