import * as React from 'react';
import {Height} from '@containers';

export default function HeightScreen({navigation,route}) {
  return (
      <Height navigation={navigation} route={route}/>
  );
}

