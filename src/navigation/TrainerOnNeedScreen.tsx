import * as React from 'react';
import {TrainerOnNeed} from '@containers';

export default function TrainerOnNeedScreen({navigation,route}) {
  return (
      <TrainerOnNeed navigation={navigation} route={route}/>
  );
}
