import * as React from 'react';
import {GenderSelection} from '@containers';


export default function GenderSelectionScreen({navigation}) {
  return (
      <GenderSelection navigation={navigation}/>
  );
}
