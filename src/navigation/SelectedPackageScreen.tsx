import * as React from 'react';
import {SelectedPackage} from '@containers';


export default function SelectedPackageScreen({navigation}) {
  return (
      <SelectedPackage navigation={navigation}/>
  );
}
