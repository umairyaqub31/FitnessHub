import * as React from 'react';
import {RoleSelection} from '@containers';


export default function RoleSelectionScreen({navigation}) {
  return (
      <RoleSelection navigation={navigation}/>
  );
}
