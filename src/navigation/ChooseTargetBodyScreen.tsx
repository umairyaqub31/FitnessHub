import * as React from 'react';
import {ChooseTargetBody} from '@containers';


export default function ChooseTargetBodyScreen({navigation}) {
  return (
      <ChooseTargetBody navigation={navigation}/>
  );
}
