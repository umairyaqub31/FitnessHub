import * as React from 'react';
import {CurrentWeight} from '@containers';

export default function CurrentWeightScreen({navigation,route}) {
  return (
      <CurrentWeight navigation={navigation} route={route}/>
  );
}

